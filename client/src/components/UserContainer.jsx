import Search from "./Search";

const UserContainer = () => {
    return (
        <section className="card users-container">
            <Search />    
        </section>
    );
};

export default UserContainer;